{
  "name": "partners-alliances-dashboard-dropbox",
  "private": true,
  "version": "12.0.0",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && esbuild blob-import-src.js --bundle --format=iife --target=es2020 --outfile=public/blob-import.js && cp index.html public/index.html && cp -R assets public/assets"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
